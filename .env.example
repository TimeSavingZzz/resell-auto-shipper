# 二手平台网页版的整串 Cookie（分号分隔的全部键值）。适配你所在平台的具体取法见 README「适配你的平台」。
# 提示：若会话由平台签名/参数保护（不只是一串 Cookie），则需改代码里的 token 换取与 WebSocket 握手，见 app/token_api.py。
# 注意：真实 COOKIES_STR 只写进项目根的 .env（已被 git 忽略），不要提交到仓库。
COOKIES_STR=your_cookies_here

# 可选：直接提供 WebSocket 注册用的 accessToken（不填则用 Cookie 换取）
# TOKEN=

# 心跳间隔 / 超时（秒）
HEARTBEAT_INTERVAL=15
HEARTBEAT_TIMEOUT=30
# 断线重连最大退避（秒）
MAX_BACKOFF=60

# ===== 功能开关 =====
# 1：检测到「等待卖家发货」事件 → 自动私发商品话术并标记已发货
AUTO_SHIP=0
# 1：成交后把同款商品补足到 min_online 份（需先配好 listing 发布模板）
AUTO_RELIST=0

# ===== Web 管理页（app/control，独立进程）=====
# 访问令牌：X-Admin-Token 请求头与之相等才放行。留空则管理页拒绝所有请求。
# 生成建议：openssl rand -hex 16
ADMIN_TOKEN=
# 管理页 HTTP 监听地址/端口（默认仅本机）
CONTROL_HOST=127.0.0.1
CONTROL_PORT=8788

# ===== 告警（app/alerter，微信/邮件）=====
# 主渠道：log（仅日志）| serverchan（Server酱）| pushplus（PushPlus）
ALERT_CHANNEL=log
# Server酱 SendKey（sctapi.ftqq.com）
ALERT_SENDKEY=
# PushPlus token（www.pushplus.plus）
ALERT_PUSHPLUS_TOKEN=
# 同一类型告警最短重复间隔（秒），防刷屏
ALERT_THROTTLE=600

# ===== 邮件附加渠道（可选的第二通道，独立于主渠道）=====
# 邮件是并列渠道：SMTP 配置齐后，告警会在微信之外再发一封邮件；
# 即使 ALERT_CHANNEL=log 也会发邮件。ALERT_EMAIL_TO 留空则不启用邮件。
# 465 = SMTP_SSL；587 = STARTTLS。PASS 是 SMTP 授权码，不是登录密码。
ALERT_SMTP_HOST=smtp.qq.com
ALERT_SMTP_PORT=465
ALERT_SMTP_USER=
ALERT_SMTP_PASS=
ALERT_EMAIL_TO=

# ===== Cookie 每日主动检测（app/cookiewatch + systemd timer）=====
# 与 bot 的被动检测互补：每天跑一次独立 token 探测，bot 停了也兜底。
# 业务性失效/风控 → 走上面告警渠道提醒；网络抖动不误报。无需额外配置。
# COOKIEWATCH_TIMER=  # （仅在 deploy/systemd/resell-cookiewatch.timer 里定执行时刻）
